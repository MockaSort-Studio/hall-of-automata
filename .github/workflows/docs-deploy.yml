name: Deploy Docs

on:
  push:
    branches:
      - main
    paths:
      - codex/**

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  docs:
    uses: MockaSort-Studio/actions/.github/workflows/docs-deploy.yml@main
    with:
      config-file: pyproject.toml
