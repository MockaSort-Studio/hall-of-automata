{
  email {$RELAY_ADMIN_EMAIL}
}

{$RELAY_DOMAIN} {
  reverse_proxy relay:3000

  log {
    output stdout
    format json
    level  INFO
  }

  # Strip client IP headers before they reach the relay — Caddy sets X-Forwarded-For
  header -X-Real-IP
}
